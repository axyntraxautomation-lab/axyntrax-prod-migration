# DASHBOARD SUPREMO AXYNTRAX

## Overview

AXYNTRAX DASHBOARD SUPREMO is an enterprise dashboard designed to centralize and streamline business operations for AXYNTRAX AUTOMATION. It integrates AI orchestration, omnichannel communication, CRM, license management, finance, email automation, and analytics. The project's main purpose is to provide a unified platform for managing client interactions, sales, financial transactions, and AI-driven tasks within a secure, user-friendly interface. The business vision is to offer a comprehensive solution for companies to manage their entire operational workflow efficiently, leveraging AI and robust communication tools.

## User Preferences

- "Ninguna implementación sin acuerdo consensuado" — confirm each fase.
- Use simple language for explanations.
- I prefer an iterative development approach.
- Ask before making major changes.
- Do not make changes to folder `lib/db/src/schema`.
- Idioma obligatorio: español latinoamericano (Perú). Prohibido inglés, español de España y voseo rioplatense. Aplica a toda copia visible al usuario (portal público, dashboard JARVIS, mensajes de error de la API que viajan al cliente, correos automáticos, textos generados por la IA, labels/aria-label/title/sr-only de formularios, tooltips, toasts, estados vacíos). NO aplica a logs internos, comentarios de código, identificadores de DB ni a marcas/términos técnicos ya peruanizados ("JARVIS", "AXYNTRAX", "Yape", "login", "dashboard", "bot", "API", "endpoint", "slide"). Reglas:
  - Anti-España: reemplazar `vosotros`→`ustedes`, `ordenador`→`computadora`/`PC`, `móvil` (teléfono)→`celular`, `fichero`→`archivo`, `vale`→`ok`/`está bien`, `coger`→`agarrar`/`tomar`, `zumo`→`jugo`, `patata`→`papa`, `ratón` (dispositivo)→`mouse`, `pulsar` (clic)→`hacer clic`.
  - Anti-voseo (Perú usa tuteo, no rioplatense): reemplazar imperativos voseo por imperativos tuteo (`escaneá`→`escanea`, `enviá`→`envía`, `usá`→`usa`, `mencioná`→`menciona`, `recordá`→`recuerda`, `cambiá`→`cambia`, `revisá`→`revisa`, `rotá`→`rota`, `reintentá`→`reintenta`, `solicitá`→`solicita`, `volvé`→`vuelve`, `respondé`→`responde`, `escribí`→`escribe`, `pedí`→`pide`, `hacé`→`haz`, `pagá`→`paga`, `depositá`→`deposita`, `esperá`→`espera`, `seguí`→`sigue`, `ignorá`→`ignora`, `contame`→`cuéntame`, `pedile`→`pídele`, etc.) y presentes voseo por presentes tuteo (`tenés`→`tienes`, `querés`→`quieres`, `podés`→`puedes`, `sabés`→`sabes`, `escribís`→`escribes`, `contás`→`cuentas`, `respondés`→`respondes`). Pronombre `vos`→`tú` (`fuiste vos`→`fuiste tú`, `vos no estás`→`tú no estás`).

## System Architecture

The project is built as a pnpm monorepo using Node.js 24 and TypeScript 5.9.

**UI/UX Decisions:**
- **Branding:** AXYNTRAX logo is consistently displayed.
- **Color Scheme:** Cyan/turquoise primary color on a dark canvas, using TailwindCSS tokens. Features a unified visual language of "aerospace cockpit / AI operations room" with specific tokens for canvas, signature cyan, plasma violet, and display fonts (Space Grotesk, JetBrains Mono).
- **Language:** Idioma obligatorio en toda la copia visible: **español latinoamericano (Perú)**.
- **No Emojis:** Emojis are explicitly excluded from the UI.
- **Frontend Framework:** React 18 with Vite 7, Wouter for routing, TanStack Query for data fetching, TailwindCSS for styling, shadcn/ui for components, and lucide-react for icons. PWA features are included.
- **Shared Primitives:** Custom `GlassCard`, `GradientButton`, `BlueprintBackdrop`, `SectionHeader`, `StatusPill`, `KpiTile`, `JarvisAvatar`, `ChatBubble`, `ChatTypingIndicator`, and `EmptyPremium` components apply a consistent premium look.

**Technical Implementations & Design Choices:**
- **API Server:** Express 5, Drizzle ORM, `bcryptjs` for password hashing, `jsonwebtoken` for JWTs, `zod/v4` for validation, `cookie-parser`, and pino for logging.
- **API Codegen:** Orval generates typed React Query hooks, Zod validators, and shared TS types from `lib/api-spec/openapi.yaml`.
- **Authentication:** JWTs in HttpOnly cookies (30-day TTL), `bcryptjs` (cost 10) for password hashing. Role-Based Access Control (RBAC) via `requireRole` middleware.
- **Two-Factor Authentication (2FA):** TOTP (RFC 6238) using `otplib` for JARVIS dashboard access, with setup flow including QR code generation. Optional 2FA for client portal admins. Includes an email-OTP fallback (rate-limited, 10-minute TTL) delivered via Gmail integration.
- **JARVIS Dashboard:** Internal rebrand as **JARVIS · IA AXYNTRAX**, serving as the central command for bots, modules, finance, telemetry, and advertising.
- **Advertising Management:** AI (Gemini 2.5 Flash) generates ad copy, normalized to JSON. A scheduler runs hourly.
- **CTA Yape:** Direct deposit details for "Yape · Miguel Montero · 991 740 590" are prominently displayed for all payments.
- **Field-Level Encryption:** AES-256-GCM encrypts sensitive fields (e.g., client phone numbers, license keys).
- **AXYN CORE (AI Chat):** SSE endpoint for streaming responses from Claude Sonnet 4.6 and Gemini 2.5 Flash. System prompt defines AXYNTRAX persona. `ai_logs` stores hashed prompt details.
- **Omnichannel Inbox:** Manages conversations from web, Meta (Facebook Messenger, Instagram DM), and WhatsApp. Webhook receivers handle incoming messages with signature verification.
- **JARVIS WhatsApp Auto-reply:** Automatically responds to WhatsApp text messages using Gemini 2.5 Flash, incorporating dynamic knowledge.
- **Dynamic JARVIS Knowledge:** `lib/jarvis-knowledge.ts` provides `BUSINESS_FACTS` and dynamically built knowledge from the active `modules_catalog`, injected into AXYN CORE, public sales bot, quote bot, and JARVIS WhatsApp.
- **Cecilia Suite (Module Management):** Manages industry-specific modules. Offers 30-day free demos for modules with `monthlyPrice = 0` with automated license key generation and expiration tracking. Paid modules require formal quotation. Uses Gemini `gemini-2.5-flash` for classification in Gmail automation.
- **Analytics:** Provides overviews of conversations, messages, financial data, AI usage, and response times, visualized with Recharts.
- **Security:** Helmet for HTTP headers, rate limiting (general and auth-specific), audit logging. Features IP blocklist, admin lockout recovery, and a global maintenance mode (`kv_settings.lockdown`).
- **2FA Rescue:** CLI command `pnpm --filter @workspace/api-server run reset-2fa <email>` to reset 2FA for blocked admins.
- **RBAC Hardening:** Comprehensive role-based access control applied across internal CRM, inbox, modules, and licenses routes.
- **Public Portal:** Separate artifact for client module activation and a lightweight admin section. Includes client accounts with self-service registration, per-module licenses, and PDF license certificate generation.
- **Hybrid Sales Model (Quotes):** Modules with `monthlyPrice > 0` require formal quotation (PDF generation with 18% IGV, single-currency enforcement, optional email delivery). Free 30-day demos for `monthlyPrice = 0`. Public landing includes a `SalesBotWidget`.
- **JARVIS Live Monitor:** Internal dashboard page with a live event feed and a JARVIS chat providing real-time context.
- **2FA Reset Stats Panel (admin):** Admin-only panel to view 2FA reset statistics from audit logs.
- **KeyGen:** Generates 30-character alphanumeric license keys.
- **Module Updates Fanout:** Admin can publish module updates, fanned out to active client modules with push notifications.
- **JARVIS Support:** AI-powered support for modules via Gemini 2.5 Flash, with persona tailored per industry.
- **Cecilia Tenant SaaS (artifact `tenant`, base `/tenant/`):** Mobile-first multi-tenant app (Vite+React+TS+Tailwind+Chart.js, viewport 375). Reuses `axyn_portal` cookie from the public portal as auth bridge — `/tenant/` redirects unauthenticated users to `/portal/login`. Flow: 3-step onboarding wizard (rubro → marca/colores/logo → WhatsApp) → dashboard adapted to the chosen rubro (KPIs, ventas en Chart.js, alertas de stock, agenda semanal, finanzas) → floating Cecilia chat (SSE) and `/tenant/faq` page with LRU cache (TTL 1h, fuzzy Jaccard ≥ 0.5) to short-circuit FAQ questions before hitting the model.
- **Cecilia Disguise (model anonymity):** All tenant-facing AI traffic goes through `artifacts/api-server/src/lib/cecilia-disguise.ts`. Defense-in-depth: (1) blindado system prompt forcing identity = "Cecilia · AXYNTRAX"; (2) streaming sanitizer with 48-char tail buffer that rewrites/strips banned tokens (case-insensitive, longest-match-first) including `gemini`, `google`, `openai`, `gpt`, `anthropic`, `claude`, `bard`, `vertex`, `deepmind`, `llama`, `mistral`; (3) re-sanitization at read time in `GET /api/tenant/cecilia/messages`. Provider names never reach the client.
- **Tenant API surface:** `GET /api/tenant/me`, `GET /api/tenant/rubros`, `POST /api/tenant/signup`, `PUT /api/tenant/branding`, `PUT /api/tenant/onboarding`, `POST /api/tenant/logo` (Supabase Storage `tenant-logos`), `POST /api/tenant/cecilia` (SSE Gemini 2.5 Flash via disguise), `GET /api/tenant/cecilia/messages`, `GET /api/tenant/faqs`. Internal `metadata` columns are never exposed to the frontend.
- **Realtime + RLS endurecido + Bandeja alertas + Reglas Cecilia (Task #47, iter 6 post-revisión):** Conteo de tablas con RLS = **18** (1 `tenants` con policy self-select por `id::text = jwt.tenant_id` + **17 `tenant_*`** con policy `*_tenant_isolation FOR ALL` USING+WITH CHECK por `tenant_id`). El extractor de claim usado en todas las policies es literalmente `current_setting('request.jwt.claims', true)::jsonb ->> 'tenant_id'` (constante `JWT_TENANT_EXPR` en `lib/db-supabase/scripts/install-rls.ts`); equivale al alias `request.jwt.claim.tenant_id` documentado por PostgREST/Supabase pero usamos la forma jsonb explícita por ser portable a Postgres puro. Nomenclatura final de reglas Cecilia: `cita_proxima | pago_pendiente | cita_completada_sin_pago | dia_sin_ventas` (tipo `pago_pendiente_24h` se renombró a `pago_pendiente` para coincidir con la spec). Banner JWT-expiry copy: "Tu sesión venció, refrescamos en un momento". Cobertura tests RLS 7/7 PASS, includendo INSERT forjado mínimo en las 17 tablas tenant_* que debe devolver 401/403 antes de evaluar NOT NULL constraints (si responde 400 sería un fallo de policy).
- **Realtime + RLS endurecido + Bandeja alertas + Reglas Cecilia (Task #47, iter 2 post-revisión):** RLS ahora con policy unificada `*_tenant_isolation` `FOR ALL` por tabla (USING + WITH CHECK por `tenant_id::text = jwt.tenant_id`) + `GRANT SELECT, INSERT, UPDATE, DELETE` a `tenant_owner`; cumple el contrato CRUD-por-tenant-claim. `tenants` queda con SELECT self-only, `rubros_registry` con SELECT público. Tests RLS extendidos (7/7 PASS) cubren además: (a) writes propios OK (INSERT con tenant_id propio + UPDATE/DELETE filtrado por tenant_id propio devuelven 2xx) y (b) writes cross-tenant DENY (INSERT con `tenant_id` ajeno forjado en body es rechazado por WITH CHECK; UPDATE/DELETE filtrado por tenant ajeno no afecta filas verificado por snapshot via service-role). Limpieza de slop: removido `lib/db-supabase/scripts/lookup-slug.cjs` (debug residual), removidos `void isNull; void desc;` de `cecilia-rules.ts` (junto con sus imports muertos), y reemplazados los casts `as unknown as "system"` / `as never` en `useRealtimeBus.ts` por los enums oficiales `REALTIME_LISTEN_TYPES.POSTGRES_CHANGES` y `REALTIME_POSTGRES_CHANGES_LISTEN_EVENT.ALL` de `@supabase/supabase-js`.

- **Realtime + RLS endurecido + Bandeja alertas + Reglas Cecilia (Task #47, iter post-validación):** throttle realtime bajado a 1 refetch/s (`MAX_PER_SEC=1` en `useRealtimeBus`); `AlertasDrawer` ahora trae botón global "Marcar todas" (`POST /api/tenant/alertas/marcar-todas-leidas`, UPDATE bulk filtrado por `tenant_id+leida=false`) y un CTA "Ver" por alerta que mapea `tipo+payload` (`stock_bajo→/inventario`, `cita_*→/agenda`, `pago_pendiente_24h→/pagos`, `dia_sin_ventas→/finanzas`) usando wouter; `AgendaCalendar` consume `/api/tenant/citas?desde&hasta` para la semana y refetcha en `tenant_citas_servicios`; `KpiTileGrid` consume `/api/tenant/finanzas/summary`+`/citas`+`/clientes`+`/pagos-qr?estado=pendiente` y refetcha en 4 tablas; `SalesChart` consume `/api/tenant/finanzas?tipo=ingreso` semanal y bucketea por día. Matriz RLS extendida a las 17 tablas `tenant_*` con asserts cross-tenant explícitos (`tenant_id=eq.<A.id>` con tokenB) — 6/6 PASS. JWT-expiry UX: `lib/supabase.ts` expone `onRealtimeStatus`/`emitRealtimeStatus` (`ok|reconnecting|down`); `useRealtimeBus` mantiene un `Map<channel,state>` y emite el peor estado vigente (banner se desbloquea apenas todos los canales vuelven a `SUBSCRIBED`); en `CHANNEL_ERROR/TIMED_OUT` invoca `args.onChannelError`; `TenantProvider.onTokenExpired` llama `rotateJwt()` con dedupe (in-flight ref + ventana de 5 s) para evitar thundering-herd entre 8 canales y `rotateJwt` empuja el nuevo JWT a Supabase con `setSupabaseAuth`. `RealtimeStatusBanner` (montado en `App.tsx`) muestra "Reconectando con el servidor…" mientras hay canales en error. Bug fix lateral: `cecilia-rules` referenciaba `tenantCitasServiciosTable.clienteNombre` (columna inexistente) lo que reventaba reglas 1 y 3 con `TypeError` en cada tick — cambiado a `titulo`.

- **Realtime + RLS endurecido + Bandeja alertas + Reglas Cecilia (Task #47):** RLS habilitado en 18 tablas (`tenants` self-only + 17 `tenant_*`) vía `lib/db-supabase/scripts/install-rls.ts` (idempotente). El rol `tenant_owner` recibe SOLO `SELECT` filtrado por `current_setting('request.jwt.claims',true)::jsonb->>'tenant_id'`; INSERT/UPDATE/DELETE quedan reservados al service-role del backend (BYPASSRLS). `rubros_registry` queda con SELECT público. Realtime: `lib/supabase.ts` singleton + `useRealtimeBus` suscribe 8 canales `tenant_*` (filter `tenant_id=eq.<id>`) con debounce 200 ms y throttle máx 5 refetch/s; bootstrap en `TenantProvider`. Componentes consumidores: `StockAlertList`, `FinanceCard`, `AlertasDrawer` (campana en `BrandingHeader` con badge no-leídas, marcado individual y "marcar todas"). `/api/tenant/me` añade `realtime:{supabaseUrl,supabaseAnonKey}`. Reglas auto Cecilia (`artifacts/api-server/src/lib/cecilia-rules.ts`, scheduler 15 min): `cita_proxima` (<1h sin recordatorio), `pago_pendiente_24h`, `cita_completada_sin_pago` (>2h tras completar), `dia_sin_ventas` (>=20h sin ingreso); dedupe por `tipo+payload-key` con `leida=false`. Tests: `pnpm --filter @workspace/api-server run test:rls` (5 casos: aislamiento cross-tenant en 4 tablas, `tenants` self-only, anon sin policy, INSERT/UPDATE/DELETE denegados, lookup por id ajeno → 0 filas). Comandos: `pnpm --filter @workspace/db-supabase run install-rls` aplica policies; el script y los tests son re-ejecutables.
- **Cecilia Business CRUDs (Task #46):** Tenant-scoped routes in `artifacts/api-server/src/routes/tenant-business.ts` for inventario (auto stock_bajo alerts when `cantidad <= minimo`), servicios/catálogo (with `tipo` discriminator: `servicio` | `producto` | `menu_item`, exposable via PATCH), empleados (CRUD nuevo: nombre/rol/color/activo, usados como recurso atendedor en agenda y wizard), clientes finales (telefono and notas encrypted at rest with AES-GCM via `encryptField`/`decryptField`; phone search uses HMAC-SHA256 hash column `telefono_hash` con índice único por `(tenant_id, telefono_hash)`; el hash se omite del payload; `GET /tenant/clientes` agrega `comprasCount`/`comprasMonto` calculados desde `tenant_finanzas_movimientos` con `tipo='ingreso'`), citas/agenda (estados normalizados a `pendiente|confirmado|en_curso|completado|cancelado|no_asistio`, con FK opcional `empleado_id`), finanzas (CRUD completo: list/create/PATCH/delete + `/finanzas/summary` día/semana/mes + canalesMes; `POST /finanzas`, `PATCH /finanzas/:id` y `POST /pagos-qr/generar` aceptan `rubroData` que se persiste como `metadata.rubro`), alertas (list + marcar leída), faq-overrides (merged into `GET /api/tenant/faqs`) and pagos QR Yape/Plin (`/pagos-qr/generar` returns `qrDataUrl` + deeplink yape://plin://; `/pagos-qr/confirmar` is race-safe idempotent — UPDATE filtered by `tenant_id+id+estado='pendiente'` — and creates the ingreso movimiento on first confirm; `GET /pagos-qr` accepts `?estado=pendiente|confirmado|anulado`). Every Supabase query carries an explicit `tenant_id` and writes are throttled by `writeLimiter` (120/min). Frontend (`artifacts/tenant`): `SideNav` con tabs adaptados por rubro (e.g. car_wash → "Insumos"/"Nuevo lavado"); pages `Inventario`/`Catalogo` (con selector tipo)/`Clientes`/`Agenda` (toggle Lista↔Calendario semanal coloreada por empleado, alta inline de empleados, picker estado con labels humanos)/`Finanzas`/`Pagos` (con status filter chips)/`FaqEditor` con form modals + Zod; `NuevaVentaWizard` adaptativo por rubro vía `lib/rubros/wizards.ts` (campos extra `placa`/`mesa`/`diagnostico`/`estilista`/etc., paso "atiende" cuando `pideEmpleado`, CTA y stepLabels personalizados, datos extra viajan al backend en `rubroData`).

## External Dependencies

- **Databases:**
    - Replit-provisioned PostgreSQL (main DB for JARVIS internal, CRM, leads, modules, internal finance).
    - Supabase PostgreSQL (independent DB for multi-tenant SaaS **Cecilia**).
- **AI Integrations:**
    - Anthropic (Claude Sonnet 4.6) via Replit AI integration.
    - Google Gemini (Gemini 2.5 Flash) via Replit AI integration.
- **Communication Platforms:**
    - Facebook Messenger (via Meta webhooks).
    - Instagram Direct Messages (via Meta webhooks).
    - WhatsApp Cloud API (via WhatsApp webhooks).
    - Gmail (via Replit Google Mail integration).
- **Payment Gateway:** Culqi (planned).
- **Tax Authority:** SUNAT (Peru's tax authority, planned).